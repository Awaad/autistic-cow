# generated by datamodel-codegen:
#   filename:  missions.schema.json

from __future__ import annotations

from enum import Enum
from typing import Any
from uuid import UUID

from pydantic import BaseModel, Field, RootModel


class MissionType(Enum):
    bar_pilgrimage = 'bar_pilgrimage'
    rescue_chain = 'rescue_chain'
    controlled_demolition = 'controlled_demolition'
    wine_hunt = 'wine_hunt'


class MissionOffer(BaseModel):
    mission_id: UUID
    mission_type: MissionType
    title: str
    brief: str
    config: dict[str, Any]
    reward_currency: int


class MissionList(BaseModel):
    missions: list[MissionOffer] = Field(..., max_length=2)


class Status(Enum):
    completed = 'completed'
    abandoned = 'abandoned'


class MissionResult(BaseModel):
    mission_id: UUID
    status: Status


class Missions(RootModel[MissionOffer | MissionList | MissionResult]):
    root: MissionOffer | MissionList | MissionResult = Field(..., title='Missions')
