{
  "version": 1,
  "_doc": "Deterministic generation parameters for the simplify stage. Kept in config (not code) per the tuning-in-data principle. Palette keys must exist in the client art/palette.ts.",
  "simplify": {
    "storey_height_m": 3.0,
    "default_levels": { "min": 2, "max": 3 },
    "height_clamp_m": { "min": 4.0, "max": 14.0 },
    "merge_margin_m": 0.4,
    "max_merge_span_m": 45.0,
    "_merge_doc": "Two footprints fuse into one collider when their boxes (grown by merge_margin_m) overlap — i.e. they share or nearly share a wall. max_merge_span_m caps the fused box so transitive chaining can't grow a whole block into one giant slab (which would render as a map-covering roof cone). Old-town terraces collapse from N boxes to a few, bringing 578 under the 350-collider budget (ADR-018).",
    "min_footprint_m": 1.0,
    "footprint_simplify_m": 1.5,
    "footprint_min_area_m2": 6.0,
    "awning_probability": 0.35,
    "palette_cluster_m": 45.0,
    "_palette_doc": "Wall colour is seeded per ~45 m block so neighbours share a tone (coherent Mediterranean zones), not a per-building rainbow. Roofs still vary per building.",
    "palette": {
      "wall": ["wallWhite", "wallCream", "wallRose"],
      "roof": ["roofTerracotta", "roofOchre"]
    }
  },
  "gameplay": {
    "grid_cell_m": 4.0,
    "child_zones": { "count": 4 },
    "camel": {
      "min_lanes": 2,
      "child_exclusion_m": 12.0,
      "lane_length_m": 90.0,
      "min_lane_len_m": 25.0,
      "_note": "camel lanes follow real streets (clear corridors); min_lane_len_m is the shortest road span that counts as a lane. child_exclusion_m mirrors shared/tuning.json (ADR-019)."
    },
    "beer": {
      "coverage_radius_m": 90.0,
      "max_spots": 28,
      "_note": "a searching player finds beer in <=45 s (01 §3.1) -> a large find-radius; capped so a player NOT looking passes 2-3 without noticing (sparse, not blanketed)."
    },
    "wine_hides": { "count": 4 },
    "rescue_spots": { "count": 8 },
    "smashables": {
      "max_dynamic": 240,
      "_doc": "A smashing game needs things to smash EVERYWHERE, not one market pen. Scattered across the whole cell: along streets (kerbside clutter) and in plazas. Hard-capped at max_dynamic (perf budget: <=250 dynamic bodies at spawn).",
      "target_dynamic": 220,
      "along_roads_frac": 0.55,
      "road_offset_m": 4.0,
      "road_spacing_m": 18.0,
      "min_gap_m": 3.0,
      "kind_weights": { "crate": 0.44, "stall": 0.2, "scooter": 0.22, "planter": 0.14 },
      "points": { "crate": 8, "stall": 24, "scooter": 18, "planter": 5 },
      "stalls_per_100m2": 0.5,
      "crates_per_100m2": 0.9,
      "scooter_count": 16
    },
    "palms": { "count": 8 }
  }
}
