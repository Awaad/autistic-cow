# ADR-006: React 19
**Status:** accepted
**Decision:** React 19 (it is stable): compiler removes manual
memoization on rage-tick HUD re-renders; Actions clean signup/consent forms.
Engine is React-free regardless (Repo Law 1), so shell framework risk is low.
